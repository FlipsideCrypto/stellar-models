version: 2
models:
  - name: silver__transactions
    columns:
      - name: ID
        description: "{{ doc('id') }}"
        tests:
          - not_null
          - unique
          - dbt_expectations.expect_column_values_to_be_in_type_list:
              column_type_list:
                - NUMBER